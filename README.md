## Pracising the  Git commands 

## git init  - 
initialize the an existing directory as a git respository 

## git status - 
get the status of the current repo or the folder 

## git add - 
add the current filw into the git hub repo  ..... so it can be

## git commit-
commit your staged contents as a newcommit snapshot

## git branch -M main
make a new branch from a master branch called as main 

## git remote add origin "URL"
git the destination to the git to oush the code

## git remote -v 
we can identify the origin and the destination of the repo

## git push origin main
give the push to the specific account 

## git credential-manager github logout <username>
to change the account from the terminal or the vs code 

## git config -- global user.name"xyz"(setup and one time)
before you give all the commands 

## git config -- global user.email "xyz@gmail.com"(setup and one time) 
before giving all the commands 



