## Pracising the  Git commands 

## git init  - 
initialize the an existing directory as a git respository 

## git status - 
get the status of the current repo or the folder 

## git add - 
add the current filw into the git hub repo  ..... so it can be

## git commit-
commit your staged contents as a newcommit snapshot

## git branch -M main
make a new branch from a master branch called as main 

## git remote add origin "URL"
git the destination to the git to oush the code

## git remote -v 
we can identify the origin and the destination of the repo

## git push origin main
give the push to the specific account 

## git credential-manager github logout <username>
to change the account from the terminal or the vs code 

## git config -- global user.name"xyz"(setup and one time)
before you give all the commands 

## git config -- global user.email "xyz@gmail.com"(setup and one time) 
before giving all the commands 

## git pull origin main 
to get the complete pull from the existing repo 

## gitignore 
which ever file we have that we  do not want to send it to the git those files are send to git ignore so that they are not added to the repo
for example if the the model is large  it should be send to git ignore because it is very very huge to upload the other files also 

## git branch 
list the number of branches of the repo 

## git branch [branch name ]
create a new branch from specific name 

## git checkout [branch name]
you can switch from one branch to another branch 

## git merege[branch]
here we should not push the code directly into the git hub repo (you can do it so if you have any kind of carsh in the local sysytem it better to give a push if we are working og the same branch few days )
then we have to merge the branch locally with main branch ahe give the push to the git to main branch 




